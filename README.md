# Automated-ETL-Pipeline
This project implements an automated end-to-end ETL pipeline using AWS serverless services. The pipeline extracts data from multiple sources, performs necessary transformations using AWS Glue, and loads the processed data into a target data store. It is completely event-driven and monitored .
